#include "main.h"
#include <stdio.h>
/**
 * mul - mupltiplies two integers
 * @a: the first integer
 * @b: the second int
 * Return: always 0
 */
int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
