#include "main.h"
/**
 * _abs - print absolute value
 * @n: Inetger to compute abs value
 * Return: the abs value of n
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
